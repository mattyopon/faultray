services:
  # Web dashboard mode
  faultzero:
    build: .
    ports:
      - "8000:8000"
    environment:
      - FAULTZERO_DB_PATH=/data/faultzero.db
      - FAULTZERO_CORS_ORIGINS=*
    volumes:
      - faultzero-data:/data
      - ./examples:/app/examples:ro
    command: ["faultzero", "serve", "--host", "0.0.0.0", "--port", "8000"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/')"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Demo mode (loads sample infra + opens dashboard)
  demo:
    build: .
    ports:
      - "8000:8000"
    environment:
      - FAULTZERO_DB_PATH=/data/faultzero.db
      - FAULTZERO_CORS_ORIGINS=*
    volumes:
      - faultzero-data:/data
    command: ["faultzero", "demo", "--web"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/')"]
      interval: 30s
      timeout: 10s
      retries: 3
    profiles:
      - demo

  # CLI mode (for running simulations)
  cli:
    build: .
    volumes:
      - faultzero-data:/data
      - ./examples:/app/examples
      - ./reports:/app/reports
    entrypoint: ["faultzero"]
    command: ["--help"]
    profiles:
      - cli

volumes:
  faultzero-data:
