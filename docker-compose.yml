services:
  # Web dashboard mode
  chaosproof:
    build: .
    ports:
      - "8000:8000"
    environment:
      - CHAOSPROOF_DB_PATH=/data/chaosproof.db
      - CHAOSPROOF_CORS_ORIGINS=*
    volumes:
      - chaosproof-data:/data
      - ./examples:/app/examples:ro
    command: ["chaosproof", "serve", "--host", "0.0.0.0", "--port", "8000"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/')"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Demo mode (loads sample infra + opens dashboard)
  demo:
    build: .
    ports:
      - "8000:8000"
    environment:
      - CHAOSPROOF_DB_PATH=/data/chaosproof.db
      - CHAOSPROOF_CORS_ORIGINS=*
    volumes:
      - chaosproof-data:/data
    command: ["chaosproof", "demo", "--web"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/')"]
      interval: 30s
      timeout: 10s
      retries: 3
    profiles:
      - demo

  # CLI mode (for running simulations)
  cli:
    build: .
    volumes:
      - chaosproof-data:/data
      - ./examples:/app/examples
      - ./reports:/app/reports
    entrypoint: ["chaosproof"]
    command: ["--help"]
    profiles:
      - cli

volumes:
  chaosproof-data:
